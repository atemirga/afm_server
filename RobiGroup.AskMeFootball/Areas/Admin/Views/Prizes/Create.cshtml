@using Microsoft.AspNetCore.Hosting
@using Models.Prizes
@using Models.Cards
@model PrizeCreateModel

@inject IHostingEnvironment HostingEnvironment

@{
    if (Model.Id.HasValue)
    {
        ViewData["Title"] = "Редактировать";
    }
    else
    {
        ViewData["Title"] = "Новый приз";
    }
}

<div class="box box-info">
    <!-- /.box-header -->
    <!-- form start -->
    <form class="form-horizontal" method="post">
        <div class="box-body">
            <input type="hidden" asp-for="Id" />
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label col-sm-4"></label>
                <div class="col-sm-8">
                    <input asp-for="Name" class="form-control" required />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="Description" class="control-label col-sm-4"></label>
                <div class="col-sm-8">
                    <textarea asp-for="Description" class="form-control" required></textarea>
                    <span asp-validation-for="Description" class="text-danger"></span>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="Price" class="control-label col-sm-4"></label>
                <div class="col-sm-8">
                    <input type="number" asp-for="Price" class="form-control" required>
                    <span asp-validation-for="Price" class="text-danger"></span>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="InStock" class="control-label col-sm-4"></label>
                <div class="col-sm-8">
                    <input type="number" asp-for="InStock" class="form-control" required>
                    <span asp-validation-for="InStock" class="text-danger"></span>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="Address" class="control-label col-sm-4"></label>
                <div class="col-sm-8">
                    <input type="text" asp-for="Address" class="form-control" required>
                    <span asp-validation-for="Address" class="text-danger"></span>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="Date" class="control-label col-sm-4"></label>
                <div class="col-sm-8">
                    <input asp-for="Date" class="form-control" required />
                    <span asp-validation-for="Date" class="text-danger"></span>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="FirstPhoneNumber" class="control-label col-sm-4"></label>
                <div class="col-sm-8">
                    <input type="number" asp-for="FirstPhoneNumber" class="form-control" required>
                    <span asp-validation-for="FirstPhoneNumber" class="text-danger"></span>
                </div>
            </div>
            <!--
        <div class="form-group">
        <label asp-for="SecondPhoneNumber" class="control-label col-sm-4"></label>
        <div class="col-sm-8">
            <input type="number" asp-for="SecondPhoneNumber" class="form-control">
            <span asp-validation-for="SecondPhoneNumber" class="text-danger"></span>
        </div>
        </div>
    -->
            <div class="form-group">
                <label asp-for="Site" class="control-label col-sm-4"></label>
                <div class="col-sm-8">
                    <input type="text" asp-for="Site" class="form-control">
                    <span asp-validation-for="Site" class="text-danger"></span>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="Facebook" class="control-label col-sm-4"></label>
                <div class="col-sm-8">
                    <input type="text" asp-for="Facebook" class="form-control">
                    <span asp-validation-for="Facebook" class="text-danger"></span>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="Twitter" class="control-label col-sm-4"></label>
                <div class="col-sm-8">
                    <input type="text" asp-for="Twitter" class="form-control">
                    <span asp-validation-for="Twitter" class="text-danger"></span>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="Instagram" class="control-label col-sm-4"></label>
                <div class="col-sm-8">
                    <input type="text" asp-for="Instagram" class="form-control">
                    <span asp-validation-for="Instagram" class="text-danger"></span>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="Vkontakte" class="control-label col-sm-4"></label>
                <div class="col-sm-8">
                    <input type="text" asp-for="Vkontakte" class="form-control">
                    <span asp-validation-for="Vkontakte" class="text-danger"></span>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="FirstPhotoUrl" class="control-label col-sm-4"></label>
                <input id="resphotosFirst" asp-for="FirstPhotoUrl" type="hidden" class="form-control" />
                <div class="col-sm-8">
                    <span class="btn btn-success fileinput-button">
                        <i class="glyphicon glyphicon-plus"></i>
                        <span>Выбрать файлы</span>
                        <!-- The file input field used as target for the file upload widget -->
                        <input id="fileuploadFirst" type="file" name="files[]" multiple>
                    </span>
                    <br>
                    <br>
                    <!-- The global progress bar -->
                    <div id="progress" class="progress">
                        <div class="progress-bar progress-bar-success"></div>
                    </div>
                    <!-- The container for the uploaded files -->
                    <table role="presentation" class="table table-striped">
                        <tbody id="filesFirst">
                            @if (Model != null && !string.IsNullOrEmpty(Model.FirstPhotoUrl))
                            {
                                <tr>
                                    <td>
                                        <img src="/@Url.Content(Model.FirstPhotoUrl)" />
                                    </td>
                                    <td>
                                        @System.IO.Path.GetFileName(Model.FirstPhotoUrl)
                                    </td>
                                    <td></td>
                                    <td>
                                        <a class="btn btn-danger" href="javascript:void(0);" onclick="deletePhotoFirst(this, encodeURI('@Model.FirstPhotoUrl'), @Model.Id)"><i class="glyphicon glyphicon-trash"></i> Удалить<a />
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <span asp-validation-for="FirstPhotoUrl" class="text-danger"></span>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="SecondPhotoUrl" class="control-label col-sm-4"></label>
                <input id="resphotosSecond" asp-for="SecondPhotoUrl" type="hidden" class="form-control" />
                <div class="col-sm-8">
                    <span class="btn btn-success fileinput-button">
                        <i class="glyphicon glyphicon-plus"></i>
                        <span>Выбрать файлы</span>
                        <!-- The file input field used as target for the file upload widget -->
                        <input id="fileuploadSecond" type="file" name="files[]" multiple>
                    </span>
                    <br>
                    <br>
                    <!-- The global progress bar -->
                    <div id="progress" class="progress">
                        <div class="progress-bar progress-bar-success"></div>
                    </div>
                    <!-- The container for the uploaded files -->
                    <table role="presentation" class="table table-striped">
                        <tbody id="filesSecond">
                            @if (Model != null && !string.IsNullOrEmpty(Model.SecondPhotoUrl))
                            {
                                <tr>
                                    <td>
                                        <img src="/@Url.Content(Model.SecondPhotoUrl)" />
                                    </td>
                                    <td>
                                        @System.IO.Path.GetFileName(Model.SecondPhotoUrl)
                                    </td>
                                    <td></td>
                                    <td>
                                        <a class="btn btn-danger" href="javascript:void(0);" onclick="deletePhotoSecond(this, encodeURI('@Model.SecondPhotoUrl'), @Model.Id)"><i class="glyphicon glyphicon-trash"></i> Удалить<a />
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <span asp-validation-for="SecondPhotoUrl" class="text-danger"></span>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="ThirdPhotoUrl" class="control-label col-sm-4"></label>
                <input id="resphotosThird" asp-for="ThirdPhotoUrl" type="hidden" class="form-control" />
                <div class="col-sm-8">
                    <span class="btn btn-success fileinput-button">
                        <i class="glyphicon glyphicon-plus"></i>
                        <span>Выбрать файлы</span>
                        <!-- The file input field used as target for the file upload widget -->
                        <input id="fileuploadThird" type="file" name="files[]" multiple>
                    </span>
                    <br>
                    <br>
                    <!-- The global progress bar -->
                    <div id="progress" class="progress">
                        <div class="progress-bar progress-bar-success"></div>
                    </div>
                    <!-- The container for the uploaded files -->
                    <table role="presentation" class="table table-striped">
                        <tbody id="filesThird">
                            @if (Model != null && !string.IsNullOrEmpty(Model.ThirdPhotoUrl))
                            {
                                <tr>
                                    <td>
                                        <img src="/@Url.Content(Model.ThirdPhotoUrl)" />
                                    </td>
                                    <td>
                                        @System.IO.Path.GetFileName(Model.ThirdPhotoUrl)
                                    </td>
                                    <td></td>
                                    <td>
                                        <a class="btn btn-danger" href="javascript:void(0);" onclick="deletePhotoThird(this, encodeURI('@Model.ThirdPhotoUrl'), @Model.Id)"><i class="glyphicon glyphicon-trash"></i> Удалить<a />
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <span asp-validation-for="ThirdPhotoUrl" class="text-danger"></span>
                </div>
            </div>
        </div>
        <!-- /.box-body -->
        <div class="box-footer">
            <button type="submit" class="btn btn-default">Отмена</button>
            <button type="submit" class="btn btn-info pull-right">Сохранить</button>
        </div>
        <!-- /.box-footer -->
    </form>
</div>

@section Styles {
    <link href="~/lib/jquery.fileupload/css/jquery.fileupload.css" rel="stylesheet" />
    <link href="~/lib/jquery.fileupload/css/jquery.fileupload-ui.css" rel="stylesheet" />
    <noscript><link href="~/lib/jquery.fileupload/css/jquery.fileupload-noscript.css" rel="stylesheet" /></noscript>
    <noscript><link href="~/lib/jquery.fileupload/css/jquery.fileupload-ui-noscript.css" rel="stylesheet" /></noscript>
}

@section Scripts
    {

    <partial name="_ValidationScriptsPartial" />
    <script src="~/lib/jquery.fileupload/js/vendor/jquery.ui.widget.js"></script>
    <!-- The Load Image plugin is included for the preview images and image resizing functionality -->
    <script src="https://blueimp.github.io/JavaScript-Load-Image/js/load-image.all.min.js"></script>
    <!-- The Canvas to Blob plugin is included for image resizing functionality -->
    <script src="https://blueimp.github.io/JavaScript-Canvas-to-Blob/js/canvas-to-blob.min.js"></script>
    <script src="~/lib/jquery.fileupload/js/jquery.iframe-transport.js"></script>
    <script src="~/lib/jquery.fileupload/js/jquery.fileupload.js"></script>
    <script src="~/lib/jquery.fileupload/js/jquery.fileupload-process.js"></script>
    <script src="~/lib/jquery.fileupload/js/jquery.fileupload-image.js"></script>
    <script src="~/lib/jquery.fileupload/js/jquery.fileupload-validate.js"></script>
    <script type="text/javascript">

        $(function () {

            $('#fileuploadFirst').fileupload({
                    url: '@Url.Action("Upload", "Photos", new{Area="Admin"})',
                    dataType: 'json',
                    acceptFileTypes: /(\.|\/)(gif|jpe?g|png)$/i,
                    maxFileSize: 999000,
                    done: function (e, data) {
                        var files = data.response().result;
                        $.each(data.files, function (index, file) {

                            var fr = $('<tr>').appendTo('#filesFirst');

                            var reader = new FileReader();

                            var imgTd = $('<td>').appendTo(fr);
                            reader.onload = function (e) {
                                $('<img>').appendTo(imgTd).attr('src', e.target.result);
                            };

                            reader.readAsDataURL(file);

                            $('<td>').text(file.name).appendTo(fr);
                            $('<td>').text(Math.round(file.size / 10) / 100 + ' KB').appendTo(fr);
                            $('<td>').html('<a class="btn btn-danger" href="javascript:void(0);" onclick="deletePhotoFirst(this, \''
                                + encodeURI(files[index].path) +
                                '\')"><i class="glyphicon glyphicon-trash"></i> Удалить<a/>')
                                .appendTo(fr);

                            $('#resphotosFirst').val(files[index].path);
                        });
                    },
                    progressall: function (e, data) {
                        var progress = parseInt(data.loaded / data.total * 100, 10);
                        $('#progress .progress-bar').css(
                            'width',
                            progress + '%'
                        );
                    }
                }).prop('disabled', !$.support.fileInput)
                .parent().addClass($.support.fileInput ? undefined : 'disabled');


        });

        function deletePhotoFirst(btn, path, rest) {

            var deletePath = decodeURI(path);
            $('#resphotosFirst').val($('#resphotosFirst').val().replace(deletePath + ';', ''));
            $(btn).closest('tr').remove();

            $.ajax({
                    method: "POST",
                    url: '@Url.Action("Delete", "Photos", new { Area = "Admin" })',
                    data: {
                        path: deletePath,
                        restaurant: rest
                    }
                })
                .done(function() {
                    //$('.btn-order').addClass('disabled');
                });

            return false;
        }



       //==========================================================================================

        $(function () {

            $('#fileuploadSecond').fileupload({
                    url: '@Url.Action("Upload", "Photos", new{Area="Admin"})',
                    dataType: 'json',
                    acceptFileTypes: /(\.|\/)(gif|jpe?g|png)$/i,
                    maxFileSize: 999000,
                    done: function (e, data) {
                        var files = data.response().result;
                        $.each(data.files, function (index, file) {

                            var fr = $('<tr>').appendTo('#filesSecond');

                            var reader = new FileReader();

                            var imgTd = $('<td>').appendTo(fr);
                            reader.onload = function (e) {
                                $('<img>').appendTo(imgTd).attr('src', e.target.result);
                            };

                            reader.readAsDataURL(file);

                            $('<td>').text(file.name).appendTo(fr);
                            $('<td>').text(Math.round(file.size / 10) / 100 + ' KB').appendTo(fr);
                            $('<td>').html('<a class="btn btn-danger" href="javascript:void(0);" onclick="deletePhotoSecond(this, \''
                                + encodeURI(files[index].path) +
                                '\')"><i class="glyphicon glyphicon-trash"></i> Удалить<a/>')
                                .appendTo(fr);

                            $('#resphotosSecond').val(files[index].path);
                        });
                    },
                    progressall: function (e, data) {
                        var progress = parseInt(data.loaded / data.total * 100, 10);
                        $('#progress .progress-bar').css(
                            'width',
                            progress + '%'
                        );
                    }
                }).prop('disabled', !$.support.fileInput)
                .parent().addClass($.support.fileInput ? undefined : 'disabled');


        });

        function deletePhotoSecond(btn, path, rest) {

            var deletePath = decodeURI(path);
            $('#resphotosSecond').val($('#resphotosSecond').val().replace(deletePath + ';', ''));
            $(btn).closest('tr').remove();

            $.ajax({
                    method: "POST",
                    url: '@Url.Action("Delete", "Photos", new { Area = "Admin" })',
                    data: {
                        path: deletePath,
                        restaurant: rest
                    }
                })
                .done(function() {
                    //$('.btn-order').addClass('disabled');
                });

            return false;
        }

        //===============================================================================


        $(function () {

            $('#fileuploadThird').fileupload({
                    url: '@Url.Action("Upload", "Photos", new{Area="Admin"})',
                    dataType: 'json',
                    acceptFileTypes: /(\.|\/)(gif|jpe?g|png)$/i,
                    maxFileSize: 999000,
                    done: function (e, data) {
                        var files = data.response().result;
                        $.each(data.files, function (index, file) {

                            var fr = $('<tr>').appendTo('#filesThird');

                            var reader = new FileReader();

                            var imgTd = $('<td>').appendTo(fr);
                            reader.onload = function (e) {
                                $('<img>').appendTo(imgTd).attr('src', e.target.result);
                            };

                            reader.readAsDataURL(file);

                            $('<td>').text(file.name).appendTo(fr);
                            $('<td>').text(Math.round(file.size / 10) / 100 + ' KB').appendTo(fr);
                            $('<td>').html('<a class="btn btn-danger" href="javascript:void(0);" onclick="deletePhotoThird(this, \''
                                + encodeURI(files[index].path) +
                                '\')"><i class="glyphicon glyphicon-trash"></i> Удалить<a/>')
                                .appendTo(fr);

                            $('#resphotosThird').val(files[index].path);
                        });
                    },
                    progressall: function (e, data) {
                        var progress = parseInt(data.loaded / data.total * 100, 10);
                        $('#progress .progress-bar').css(
                            'width',
                            progress + '%'
                        );
                    }
                }).prop('disabled', !$.support.fileInput)
                .parent().addClass($.support.fileInput ? undefined : 'disabled');


        });

        function deletePhotoThird(btn, path, rest) {

            var deletePath = decodeURI(path);
            $('#resphotosThird').val($('#resphotosThird').val().replace(deletePath + ';', ''));
            $(btn).closest('tr').remove();

            $.ajax({
                    method: "POST",
                    url: '@Url.Action("Delete", "Photos", new { Area = "Admin" })',
                    data: {
                        path: deletePath,
                        restaurant: rest
                    }
                })
                .done(function() {
                    //$('.btn-order').addClass('disabled');
                });

            return false;
        }


    </script>
}
